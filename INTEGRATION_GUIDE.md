# Интеграционный Pipeline - Руководство

Интеграция существующего workflow с новыми модулями (конвертер и БД).

## Архитектура

```
Существующие скрипты           Новые модули
━━━━━━━━━━━━━━━━━━━━          ━━━━━━━━━━━━━
parse_january_by_day_and_court.py → JSON метаданные
                    ↓
download_all_electronic_case_docs.py → PDF документы
                    ↓
         integrate_pipeline.py ←─── src/converter/pdf_to_md.py
                    ↓          ←─── src/database/sqlite_manager.py
                 MD + БД
```

## Использование

### Вариант 1: Обработка одного дела (тест)

```bash
# 1. Запустить Chrome с CDP
bash scripts/start_chrome_debug_linux.sh

# 2. В другом терминале - обработать первое дело из JSON
python3 scripts/integrate_pipeline.py \
  --json data/january_2024_cases.json \
  --db data/kad_2024.db \
  --max-cases 1
```

### Вариант 2: Обработка всех дел из JSON

```bash
python3 scripts/integrate_pipeline.py \
  --json data/january_2024_cases.json \
  --db data/kad_2024.db
```

### Вариант 3: Продолжить с определенного индекса

```bash
# Если прервалось на деле #50, продолжить с него
python3 scripts/integrate_pipeline.py \
  --json data/january_2024_cases.json \
  --db data/kad_2024.db \
  --start-index 50
```

### Вариант 4: Кастомные директории

```bash
python3 scripts/integrate_pipeline.py \
  --json data/january_2024_cases.json \
  --db data/kad_2024.db \
  --downloads /tmp/pdfs \
  --documents /mnt/storage/documents
```

## Что делает скрипт

Для каждого дела из JSON:

1. **Скачивание PDF** (ваш код)
   - Открывает страницу дела
   - Переходит на вкладку "Электронное дело"
   - Скачивает все PDF со всех страниц (с пагинацией)
   - Retry: 3 попытки на каждый документ
   - Сохраняет в `downloads/case_number/`

2. **Конвертация PDF → MD** (мой модуль)
   - Извлекает текст из PDF (pdfplumber)
   - Конвертирует в Markdown
   - Сохраняет в `documents/YYYY/case_number/`

3. **Сохранение в БД** (мой модуль)
   - Добавляет метаданные дела в таблицу `cases`
   - Добавляет документы в таблицу `documents`
   - Дедупликация автоматическая (PRIMARY KEY)

4. **Очистка** (экономия места)
   - Удаляет PDF файлы после конвертации
   - Остаются только MD файлы

## Параметры

- `--json` (обязательно) - JSON файл с делами от вашего парсера
- `--db` (обязательно) - SQLite база данных
- `--downloads` (опционально) - папка для временных PDF (по умолчанию: `downloads/`)
- `--documents` (опционально) - папка для MD документов (по умолчанию: `documents/`)
- `--start-index` (опционально) - начать с дела № N (для возобновления)
- `--max-cases` (опционально) - обработать максимум N дел (для теста)

## Результаты

После выполнения:

```
Kad_parcer/
├── data/
│   └── kad_2024.db           # SQLite БД с метаданными
├── documents/                 # MD документы
│   └── 2024/
│       ├── А40-12345-24/
│       │   ├── 001_document1.md
│       │   ├── 002_document2.md
│       │   └── ...
│       └── СИП-99_2024/
│           ├── 001_решение.md
│           └── ...
└── downloads/                 # Удаляются после конвертации (пусто)
```

## Производительность

- **Скачивание:** ~7 сек на дело (rate limiting)
- **Конвертация:** ~83 PDF/сек (параллельно)
- **Всего:** ~10-15 сек на дело (в среднем 4 документа)

**Для 40,000 дел (ноябрь 2025):**
- Время: 3-5 дней
- Документов: ~160,000
- MD файлы: ~8 GB

## Rate Limiting

Скрипт автоматически соблюдает паузы:
- 3 секунды между делами
- 2 секунды между retry попытками
- Внутри `download_case_documents` - логика пагинации с паузами

## Обработка ошибок

- ❌ Дело без "Электронное дело" → пропускается
- ❌ Ошибка скачивания PDF → retry 3 раза → пропускается
- ❌ Ошибка конвертации → пропускается, остальные обрабатываются
- ✅ Критическая ошибка → дело пропускается, обработка продолжается

## Мониторинг

Консольный вывод показывает:
```
[1/100]
================================================================================
ОБРАБОТКА ДЕЛА: А40-12345-24
================================================================================

📋 Дело: А40-12345-24
🔗 URL: https://kad.arbitr.ru/Card/...
   ✅ 125 KB → 001_решение.pdf
   ✅ 89 KB → 002_постановление.pdf
✅ Скачано документов: 2

🔄 Конвертация 2 PDF → Markdown...
   ✅ 001_решение.pdf → 001_решение.md
   ✅ 002_постановление.pdf → 002_постановление.md

📊 Конвертация завершена: 2 успешно, 0 ошибок

💾 Сохранение в БД...
   ✅ Дело добавлено: А40-12345-24
   ✅ Документов добавлено: 2

🗑️  PDF удалены (экономия места)

✅ ДЕЛО ОБРАБОТАНО: А40-12345-24
```

## Сравнение с process_cases.py

| Функция | process_cases.py | integrate_pipeline.py |
|---------|------------------|----------------------|
| Подход | Новый pipeline с поиском | Ваш workflow + мои модули |
| Парсинг | Через search input | Ваш parser (дата + суд) |
| Скачивание | Новый downloader | Ваш download script |
| Конвертация | ✅ Мой модуль | ✅ Мой модуль |
| База данных | ✅ Мой модуль | ✅ Мой модуль |
| Checkpoint | ✅ JSON файл | Простой `--start-index` |

**Вывод:** `integrate_pipeline.py` использует ваши проверенные скрипты и просто добавляет к ним конвертацию и БД.

## Тестовый запуск

```bash
# 1. Терминал 1: Запустить Chrome
bash scripts/start_chrome_debug_linux.sh

# 2. Терминал 2: Тест на 1 деле
python3 scripts/integrate_pipeline.py \
  --json data/january_2024_cases.json \
  --db data/test.db \
  --max-cases 1

# 3. Проверить результаты
ls -lh documents/2024/*/
sqlite3 data/test.db "SELECT * FROM cases;"
sqlite3 data/test.db "SELECT * FROM documents;"
```

## Полный workflow для ноября 2025

```bash
# ШАГ 1: Парсинг (ваш скрипт)
# Модифицировать parse_january_by_day_and_court.py для ноября 2025
python3 scripts/parse_november_2025.py  # Создать этот скрипт
# Результат: data/november_2025_cases.json

# ШАГ 2: Интеграционный pipeline
python3 scripts/integrate_pipeline.py \
  --json data/november_2025_cases.json \
  --db data/kad_2025.db
# Результат: documents/2025/ + kad_2025.db

# ШАГ 3: Анализ
sqlite3 data/kad_2025.db
> SELECT COUNT(*) FROM cases;
> SELECT COUNT(*) FROM documents;
> SELECT court, COUNT(*) FROM cases GROUP BY court;

# ШАГ 4: Full-text search
rg "мировое соглашение" documents/2025/
```

## Следующие шаги

1. ✅ Протестировать `integrate_pipeline.py` на 1 деле
2. ✅ Протестировать на 10 делах
3. ✅ Создать `parse_november_2025.py` (модификация вашего парсера)
4. ✅ Запустить на полном ноябре 2025 (~40,000 дел)
5. ✅ Оценить производительность и качество
6. ✅ Масштабировать на весь 2025 год

## Troubleshooting

### Chrome не подключается
```bash
ps aux | grep chrome | grep remote-debugging-port
# Если нет - перезапустить
pkill chrome
bash scripts/start_chrome_debug_linux.sh
```

### JSON не найден
```bash
# Проверить путь
ls -la data/*.json
# Использовать полный путь
python3 scripts/integrate_pipeline.py --json /full/path/to/file.json ...
```

### Нехватка места
```bash
# Использовать tmpfs для downloads
python3 scripts/integrate_pipeline.py \
  --downloads /dev/shm/pdfs \
  ...
```

### База данных заблокирована
```bash
# Закрыть другие подключения
lsof data/kad_2024.db
# Или использовать другой файл БД
```
