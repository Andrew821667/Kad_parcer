# 🚀 Руководство по запуску парсинга

## Быстрый старт

### Фаза 1: Тестовый запуск (РЕКОМЕНДУЕТСЯ НАЧАТЬ С ЭТОГО!)

Перед полным парсингом протестируйте систему на кассационном суде за 2024 год:

```bash
python scripts/parse_moscow_5years.py --test --workers 3
```

**Что произойдет:**
- Запустятся 3 параллельных браузера
- Спарсится суд А40-КС (кассация) за 2024 год
- ~30,000 дел
- Время: 1-2 дня
- Проверите качество данных и скорость

**Ожидаемый результат:**
- Браузеры откроются (в headless режиме)
- Прогресс будет отображаться в терминале
- Данные сохранятся в PostgreSQL
- Создастся файл `parsing_progress.json` с чекпоинтами

---

### Фаза 2: Полный парсинг (5 лет, 18 судов)

После успешного теста запустите полный парсинг:

```bash
python scripts/parse_moscow_5years.py --workers 10 --headless
```

**Что произойдет:**
- 10 параллельных браузеров
- 18 судов × 60 месяцев = 1,080 задач
- ~1.75 миллионов дел
- Время: ~15 дней непрерывной работы

---

## Параметры запуска

### Основные опции:

```bash
--workers, -w NUMBER       # Количество параллельных браузеров (по умолчанию: 5)
--headless / --no-headless # Headless режим (по умолчанию: включен)
--checkpoint, -c FILE      # Файл для сохранения прогресса
--test                     # Тестовый режим (только кассация 2024)
--resume                   # Продолжить с последнего чекпоинта
```

### Примеры:

**Тест с видимыми браузерами:**
```bash
python scripts/parse_moscow_5years.py --test --no-headless --workers 2
```

**Быстрый парсинг (много браузеров):**
```bash
python scripts/parse_moscow_5years.py --workers 15 --headless
```

**Продолжить после остановки:**
```bash
python scripts/parse_moscow_5years.py --resume --workers 10
```

**Свой файл прогресса:**
```bash
python scripts/parse_moscow_5years.py --checkpoint my_progress.json --workers 10
```

---

## Системные требования

### Минимальные:
- **CPU:** 4 ядра
- **RAM:** 8 GB
- **Диск:** 20 GB свободного места
- **Сеть:** Стабильное подключение к интернету

### Рекомендуемые:
- **CPU:** 8+ ядер (M4 Pro отлично подходит!)
- **RAM:** 16 GB
- **Диск:** 50 GB (с запасом для логов)
- **Сеть:** Быстрое стабильное подключение

### Количество workers:

| Конфигурация | Workers | RAM usage | Скорость |
|--------------|---------|-----------|----------|
| Минимальная  | 3-5     | ~6 GB     | ~5,000 дел/час |
| Оптимальная  | 8-10    | ~12 GB    | ~10,000 дел/час |
| Максимальная | 15-20   | ~20 GB    | ~15,000 дел/час |

**Для MacBook M4 Pro рекомендую: 10 workers**

---

## Перед запуском

### 1. Установите Playwright браузеры:

```bash
playwright install firefox
```

Это скачает Firefox для автоматизации (~200 MB).

### 2. Проверьте PostgreSQL:

```bash
# Убедитесь что PostgreSQL запущен
psql -U postgres -c "SELECT version();"

# Создайте базу данных если нужно
createdb kad_arbitr
```

### 3. Запустите миграции:

```bash
alembic upgrade head
```

Это создаст необходимые таблицы в базе.

### 4. (Опционально) Запустите Redis для Celery:

```bash
# macOS (через Homebrew)
brew services start redis

# Или через Docker
docker run -d -p 6379:6379 redis:alpine
```

*(Redis нужен только если планируете использовать Celery задачи)*

---

## Мониторинг парсинга

### Прогресс в терминале:

Скрипт показывает реалтайм статистику:

```
✓ 245 | ✗ 3 | ⏳ 832 | 📊 125,430 дел
```

- ✓ - завершенные задачи
- ✗ - задачи с ошибками
- ⏳ - задачи в очереди
- 📊 - всего дел спарсено

### Файл прогресса (parsing_progress.json):

```json
{
  "timestamp": "2025-12-09T15:30:00Z",
  "total_cases": 125430,
  "completed_tasks": [
    {
      "court_code": "А40-КС",
      "date_from": "2024-01-01",
      "date_to": "2024-01-31",
      "status": "completed",
      "cases_count": 2543
    }
  ],
  "failed_tasks": []
}
```

### Логи:

Structlog пишет подробные логи:

```bash
# Смотреть в реальном времени
tail -f logs/kad_parser.log

# Фильтровать ошибки
grep ERROR logs/kad_parser.log
```

---

## Остановка и возобновление

### Остановить парсинг:

Нажмите `Ctrl+C` в терминале.

Прогресс автоматически сохранится в `parsing_progress.json`.

### Возобновить парсинг:

```bash
python scripts/parse_moscow_5years.py --resume --workers 10
```

Парсинг продолжится с того места, где остановился.

---

## Проверка данных

### Посмотреть количество дел в БД:

```bash
psql -U postgres kad_arbitr -c "SELECT COUNT(*) FROM cases;"
```

### Посмотреть последние дела:

```bash
psql -U postgres kad_arbitr -c "SELECT case_number, court, status FROM cases ORDER BY id DESC LIMIT 10;"
```

### Статистика по судам:

```bash
psql -U postgres kad_arbitr -c "SELECT court, COUNT(*) as count FROM cases GROUP BY court ORDER BY count DESC;"
```

---

## Решение проблем

### Проблема: HTTP 451 ошибки

**Симптом:** Много задач падают с ошибкой "HTTP 451"

**Решение:**
1. Уменьшите количество workers: `--workers 3`
2. Увеличьте задержки в коде (`base_delay=(5.0, 8.0)`)
3. Перезапустите роутер (новый IP)

### Проблема: Браузеры не запускаются

**Симптом:** Ошибка "Browser not found"

**Решение:**
```bash
# Установите браузеры заново
playwright install firefox
```

### Проблема: Нехватка памяти

**Симптом:** Система тормозит, браузеры вылетают

**Решение:**
1. Уменьшите workers: `--workers 5`
2. Закройте другие приложения
3. Перезапустите Mac

### Проблема: Соединение с БД потеряно

**Симптом:** Ошибки "connection refused"

**Решение:**
1. Проверьте PostgreSQL: `brew services list`
2. Перезапустите: `brew services restart postgresql`
3. Возобновите парсинг: `--resume`

---

## Оптимизация скорости

### Для максимальной скорости:

1. **Больше workers:**
   ```bash
   --workers 15
   ```

2. **Облачный сервер:**
   - AWS EC2 (m5.2xlarge)
   - DigitalOcean Droplet (8 CPU, 16 GB RAM)
   - Стоимость: ~$50/месяц

3. **Уменьшение задержек:**
   Отредактируйте `src/scraper/playwright_scraper.py`:
   ```python
   base_delay=(2.0, 3.0)  # Вместо (3.0, 5.0)
   ```

   ⚠️ **Внимание:** Может увеличить риск блокировки!

4. **Параллельный запуск на разных машинах:**
   - Разделите суды между несколькими компьютерами
   - Каждый парсит свою часть
   - Объедините базы в конце

---

## Дальнейшие шаги после парсинга

### 1. Валидация данных

```bash
# Запустите валидацию
python scripts/validate_data.py
```

### 2. Обогащение метаданными

```bash
# Извлеките судей, статьи законов, категории
python scripts/enrich_metadata.py
```

### 3. Экспорт данных

```bash
# Экспорт в CSV
python scripts/export_data.py --format csv --output data.csv

# Экспорт в JSON
python scripts/export_data.py --format json --output data.json
```

### 4. Масштабирование

После успеха с 5 годами:
- Добавьте 2015-2019 (еще 5 лет)
- Или переходите на другие округа РФ

---

## FAQ

**Q: Можно ли запускать на Windows?**
A: Да, скрипт кроссплатформенный. Нужен Python 3.11+ и PostgreSQL.

**Q: Безопасно ли это?**
A: Да, мы используем метод, проверенный 14+ разработчиками на GitHub.

**Q: Могут ли заблокировать IP?**
A: Возможно после ~1000-5000 запросов. Решение: перезапуск роутера или прокси.

**Q: Сколько стоит облачный сервер?**
A: ~$50/месяц за сервер с 8 CPU и 16 GB RAM.

**Q: Можно ли парсить другие округа?**
A: Да! Просто измените список судов в скрипте.

**Q: Где хранятся данные?**
A: В PostgreSQL базе данных. По умолчанию: `kad_arbitr` на localhost.

---

## Поддержка

При проблемах:

1. Проверьте логи: `tail -f logs/kad_parser.log`
2. Проверьте чекпоинт: `cat parsing_progress.json`
3. Проверьте БД: `psql -U postgres kad_arbitr`

**Удачного парсинга! 🚀**
